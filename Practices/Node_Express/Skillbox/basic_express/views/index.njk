<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>({{count}}) Counter</title>
        {{uikitCss | safe}}
        <link rel='icon' type="image/png" href="/clock.png"/>
    </head>
    <body>
        <section class="uk-container">
            <h1>The count is {{count}}</h1>

            {% if count > 10 %}
            <h2>Woah!</h2>
            {% endif %}

            <p><button id="button">Increment it</button></p>
            <p>
                This page is useing UIKit:
                <code>{{uikitCss}}</code>
            </p>
            {% if count > 0 %}
            <ul>
                {% for cnt in counts %}
                <li>
                    {{cnt}} bottles of bear on the wall
                </li>
                {% endfor %}
            </ul>
            {% endif %}
        </section>
        <script>
            document.getElementById("button").addEventListener("click", () => {
                fetch("/inc", { method: "post" }).then(() => {
                    location.reload()
                })
            })
        </script>
    </body>
</html>
